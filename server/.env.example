# Example server/.env file
# Copy this to server/.env and replace with real values

# Server settings
PORT=5000
NODE_ENV=development

# MongoDB connection string (mongodb+srv://user:pass@cluster0.mongodb.net/dbname)
MONGODB_URI=REPLACE_WITH_YOUR_MONGODB_URI

# JWT secret
JWT_SECRET=REPLACE_WITH_A_RANDOM_SECRET

# OpenAI API key
OPENAI_API_KEY=sk-REPLACE_ME
